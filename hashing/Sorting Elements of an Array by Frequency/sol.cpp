#include<bits/stdc++.h>
using namespace std;
vector<int> frqsort(int a[],int n){
    vector<int> v;
    map<int,int> v;
    for(int i=0;i<n;i++){
        v[a[i]]++;
    }
    for(auto e:v){
        
    }
}
int main(){
    int a[]={};
    int n=sizeof(a)/sizeof(a[0]);
    frqsort(a,n);
}