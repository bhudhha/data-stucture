#include <bits/stdc++.h>
using namespace std;
int sortAbs(int x[], int y[], int m, int n)
{
    long long count = 0;
    sort(y, y + n);
    for(int i=0;i<m;i++){
    
    }
    return count;
}
int main()
{
    int x[] = {2, 1, 6};
    int y[] = {1, 5};

    int m = sizeof(x) / sizeof(x[0]);
    int n = sizeof(y) / sizeof(y[0]);
    sortAbs(x, y, m, n);
}